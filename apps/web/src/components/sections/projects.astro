---
import { data } from "@cv/data";
import { ExternalLinkIcon } from "@lucide/astro";
import Badge from "@web/components/ui/badge.astro";
import Section from "@web/components/ui/section.astro";
import SectionBody from "@web/components/ui/section-body.astro";
import SectionTitle from "@web/components/ui/section-title.astro";
---

<Section>
  <SectionTitle href="#projects">Projects</SectionTitle>
  <SectionBody>
    {
      data.projects.map((project) => (
        <>
          <div class="flex flex-col gap-2 md:flex-row md:items-center md:gap-4">
            <a
              href={project.url}
              target="_blank"
              rel="noopener noreferrer"
              class="flex items-center gap-1 hover:underline"
            >
              <h3 class="text-title font-mono font-bold">{project.name}</h3>
              <ExternalLinkIcon class="print:hidden" size="16" />
            </a>
            <div class="flex flex-wrap gap-2">
              {project.tech.map((tech) => (
                <Badge>{tech}</Badge>
              ))}
            </div>
          </div>
          <p class="text-sm whitespace-break-spaces">{project.description}</p>
        </>
      ))
    }
  </SectionBody>
</Section>
